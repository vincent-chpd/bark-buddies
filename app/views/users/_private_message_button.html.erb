<div>
  <%= button_to conversations_path(recipient_id: user.id, sender_id: current_user.id), method: :post, class: 'btn-profile' do %>
    <i class="fas fa-envelope"></i><span class="mx-2">Message</span>
  <% end %>
</div>
