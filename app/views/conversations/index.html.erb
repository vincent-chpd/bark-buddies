<div class="ui segment">
  <h3>Mailbox</h3>
  <div class="ui list">
    <div class="item">
      <ul>
      <% @conversations.each do |conversation| %>
        <li><%= link_to conversation.sender.name, conversation_messages_path(conversation) %></li>
      <% end %>
      </ul>
    </div>
  </div>
</div>
